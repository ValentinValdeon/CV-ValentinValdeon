
// Animacion de las Skills
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk1');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk2');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk3');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk4');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk5');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk6');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })
    
    window.addEventListener('scroll', function(){
        let animacion1 = document.getElementById('sk7');
        let posicionSk = animacion1.getBoundingClientRect().top-10;
        let tamanioPantalla = window.innerHeight;
    
        if(posicionSk < tamanioPantalla){
            animacion1.style.animation = 'mover 1.4s ease-out';
        }
    })